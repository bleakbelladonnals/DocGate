export class ApiError extends Error{constructor(public code:string,message:string,public details?:unknown){super(message)}}
export async function api<T>(path:string,init?:RequestInit):Promise<T>{const method=init?.method?.toUpperCase()??"GET";const headers=new Headers(init?.headers);if(method!=="GET"&&method!=="HEAD")headers.set("x-docgate-request","1");if(init?.body)headers.set("content-type","application/json");const response=await fetch(`/api/v1${path}`,{...init,headers,cache:"no-store"});const body=await response.json().catch(()=>({error:{code:"INVALID_RESPONSE",message:"服务返回了不可读取的数据。"}}));if(!response.ok)throw new ApiError(body.error?.code??"REQUEST_FAILED",body.error?.message??"请求失败。",body.error?.details);return body as T}
