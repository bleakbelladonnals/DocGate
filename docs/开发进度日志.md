# DocGate 第 1 阶段开发进度日志

## 检查点 1｜只读自检与外部契约

- 已完成：完整读取指定优先级文档；确认仓库最初只有文档且不是 Git 仓库；确认 Python 3.11.15、Node v22.23.1、npm 10.9.8；检查监听端口；初始化 Git 并配置忽略项。
- 实际命令与结果：`python3.11 --version`、`node --version`、`npm --version` 均满足；`git rev-parse` 初始返回非仓库，之后 `git init` 成功；`lsof -nP -iTCP -sTCP:LISTEN` 确认 3000/8765/19432 初始未占用。
- 外部契约：官方仓库 `backnotprop/plannotator`，锁定 release `v0.24.2` / commit `9bf46e11f30755b60c0eb392362fce3eaaa1966c`；macOS arm64 SHA-256 `d590a1b786b1299a25d228b30d5b97307a73b4f8f3c4ccade59f40b02f89f15d`；命令 `.tools/plannotator annotate fixtures/simple.md --gate --json`；输出为单行 `{decision, feedback}` JSON，feedback 是结构化 Markdown。
- 仍存在的风险：只在 macOS arm64 验证该二进制；其他系统需重新锁定资产。
- 下一步：实现领域、持久化、CLI/API 和 Web 纵向链路。

## 检查点 2｜后端与失败注入

- 已完成：不可变 Session/Round、原子 JSON、单会话锁、路径限制、批注适配、稳定任务、版本化 brief/schema、receipt 校验、真实行级 Diff、多对多归因、DG 规则、人工决定、返工和接受门槛；CLI 与 `/api/v1` 共用服务；Host/Origin/自定义写请求头防护。
- 实际命令与结果：首次 `.venv/bin/pytest` 为 `31 passed`；加入真实 Plannotator fixture 后为 `32 passed`。`backend/tests/test_failure_injection.py` 保留 FI-01 至 FI-15 的逐项测试名并全部通过。
- 仍存在的风险：P0 行级 Diff 对章节移动只提供低置信/未归因；不做语义完成判断。
- 下一步：完成正式最小 Web、组件测试与 E2E。

## 检查点 3｜前端与浏览器自动化

- 已完成：`/sessions` 与 `/sessions/[id]`；加载/空/错误状态；证据三分层；任务和未声明修改决定；返工/接受操作；键盘焦点、label 和非纯颜色状态；列表进度摘要。
- 实际命令与结果：`npm run typecheck` 通过；`npm run lint` 首轮发现 React effect 写法问题，修复后通过；新增 E2E 文件后 `npm test` 一度误收集 Playwright suite，限定 Vitest include 后为 `2 passed`；`npm run build` 通过；`npm run test:e2e` 首轮 1/3 通过并暴露等待/定位问题，修复后 `3 passed`。
- E2E 覆盖：空/错误状态、真实证据卡、未完成接受阻止、逐任务/未声明修改决定、刷新恢复、新 API 进程读取落盘决定、accepted 后禁写、返工只保留失败项并创建新 Round。
- 仍存在的风险：移动端只承诺可读；正式高效验收目标为 1280px。
- 下一步：真实 Plannotator 冒烟与独立浏览器目视检查。

## 检查点 4｜真实 Plannotator 与浏览器检查

- 已完成：官方二进制项目本地安装、版本和 SHA 校验；真实 `fixtures/simple.md` annotate 服务；Plannotator UI 中确认五条批注逐项存在；点击 Send Feedback；CLI 返回 `decision=annotated` 和五条带选中文本反馈；真实输出固化为公开 fixture；DocGate 适配器导入得到 5 个 unique locator task。
- 实际命令与结果：`./scripts/install-plannotator-local.sh` 输出 `plannotator 0.24.2`；`.venv/bin/python scripts/verify-plannotator-contract.py` 输出 `tasks: 5` 且五个 locator 均为 `unique`。
- 浏览器结果：Ego Chromium 在 1280×851 检查会话列表和验收页；确认会话进度、未声明修改数、before/after、Agent 声明、DG 检查和人工决定分区；不存在会话显示安全错误与重试。写操作完整状态由 Playwright 同构建验证。
- 仍存在的风险：应用内 Browser 运行时当时无可用实例，改用已提供的 ego-browser 独立 Chromium 完成目视检查；不影响产品运行或 Playwright 结果。
- 下一步：最终全量回归、README/人工清单、日志与 Git 审计、端口清理。

## 最终检查点

- 已完成能力：docs/03 要求的 CLI、`/api/v1`、Session/Round 文件协议、两张正式核心页面、Plannotator 适配与通用导入回退均已实现；README、恢复说明和产品经理人工清单已完成；依赖由 `requirements.lock` 与 `frontend/package-lock.json` 锁定。
- 最终实际命令和结果（2026-08-20）：
  - `.venv/bin/pytest` → `32 passed in 0.32s`；FI-01 至 FI-15 全部在同一套测试中通过，并覆盖格式错误及日历值非法的 UTC 时间戳。
  - `DOCGATE_WORKSPACE_ROOT="$PWD" .venv/bin/docgate doctor` → Python 3.11.15、Node v22.23.1、npm 10.9.8、Plannotator 0.24.2、端口和工作区全部通过。
  - `.venv/bin/python scripts/verify-plannotator-contract.py` → `tasks=5`，五个 locator 均为 `unique`。
  - `npm run typecheck` → 通过；`npm run lint` → 通过；`npm test` → `2 passed`；`npm run build` → production build 通过。
  - `env -u NODE_TLS_REJECT_UNAUTHORIZED npm run test:e2e` → `3 passed (10.7s)`，测试结束后 Web/API 服务自动退出；同样清除该宿主变量后的 production build 通过且无 TLS 警告。
  - Git/安全审计：秘密模式扫描无结果；`.env`、`.venv`、`.tools`、`node_modules`、`.next`、`.docgate`、`test-results`、缓存、日志、egg-info 和 tsbuildinfo 均被忽略；3000/8765/8766/19432 均无监听进程。
- 仍存在的失败/风险：没有未通过的适用自动化或外部集成项。宿主终端当前预设了不安全的 `NODE_TLS_REJECT_UNAUTHORIZED=0`，README 已给出取消命令，项目未写入此设置；三份真实长文档人工验收必须由产品经理亲自完成，不能由自动化代替；其他平台的 Plannotator 二进制仍需各自验证。
- 下一步：按 `docs/产品经理三份真实文档人工验收清单.md` 完成三份真实文档并记录时间与是否需要全文重读。
